import 'package:flutter/material.dart';

Icon arrowBackIcon = const Icon(
  Icons.arrow_back_ios_new_outlined,
);

Icon blackArrowBackIcon = const Icon(
  Icons.arrow_back_ios_new_outlined,
  color: Colors.black,
);

Icon arrowDownIcon = const Icon(
  Icons.keyboard_arrow_down_outlined,
);

Icon arrowForwardIcon = const Icon(
  Icons.arrow_forward_ios_outlined,
  size: 18,
  color: Colors.grey,
);

Icon threeDotsIcon = const Icon(
  Icons.more_horiz,
  size: 30,
);
